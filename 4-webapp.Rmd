
# Using the Web Application {#webapp}

We have created a web application which can be found here:

```{r, eval=TRUE, echo=FALSE}
library(htmltools)
rawHTML <- paste(readLines("html/section-4/webapp.html"), collapse="\n")
HTML(rawHTML)
```
